server {
  listen 80;
  listen [::]:80;
  server_name e-notes.org;

  root /usr/share/nginx/;
  index index.html;

  location =/ {
    try_files /index.html =404;
  }

  location / {
    try_files $uri /html/$uri.html =404;
  }

  location =/meta.json {
        root /usr/share/nginx/;
  }
}
